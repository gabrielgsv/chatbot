# Database Configuration
DB_HOST
DB_PORT
DB_USERNAME
DB_PASSWORD
DB_DATABASE
DB_SSL

# Database Synchronization (WARNING: Only enable for development!)
DB_SYNCHRONIZE

# JWT Configuration
JWT_SECRET

# OpenAI API Key (optional - if not provided, will use mock responses)
OPENAI_API_KEY

# Server Configuration
PORT
NODE_ENV
